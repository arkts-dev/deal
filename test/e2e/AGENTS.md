# End-to-End Tests

Compile real manifested projects through the CLI and execute produced artifacts. Avoid direct backend internals and checkout leakage. Assert observable output and failures. Expand semantic-IR production coverage across all backends as migration proceeds. Architectural direction never expands assigned task scope. Keep this file always synchronized with code.
