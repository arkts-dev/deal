# Release Tests

Verify installed distributions outside the checkout through packaged launchers, runtimes, declarations, and standard libraries. Prevent source-tree or classpath leakage. Assert real artifacts and observable execution, not implementation internals. Keep this file always synchronized with code.
