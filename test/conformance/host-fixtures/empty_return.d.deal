// Host fixture declaration for the host-empty-return-bad conformance test.
// The implementation returns zero results for a non-null declared return —
// the wrapper's presence rule must raise E8010 on every call path,
// including the discard path (host-module-abi D3 case 2).

export function ping(): int;
