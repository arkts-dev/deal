// @expected: host-fixture
// @description: Host-module declaration fixture (host-module-abi D6) —
// resolved on demand by the host-fixture registry; the discovery walk
// skips test/conformance/host-fixtures/ entirely.
// Host fixture declaration for the host-bad-return conformance test. The
// raw implementation returns a string where the declared return type is
// int — the wrapper must raise E8010 at the call site (R1).

export function getNumber(): int;
