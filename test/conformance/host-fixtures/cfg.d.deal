// @expected: host-fixture
// @description: Host-module declaration fixture (host-module-abi D6) —
// resolved on demand by the host-fixture registry; the discovery walk
// skips test/conformance/host-fixtures/ entirely.
// Host fixture declaration for the host-class-export conformance test
// (host-module-abi D6 + runtime-class-identity D1-D2). The class exports
// carry the canonical externals identity (@$external/host.cfg/...) and the mandatory
// <C>_defaults tables (construction depends on them); the optional
// same-module class field exercises the synthesized ClassSymbol resolution
// (NamedType over a registered host class). Class identity strings equal
// the canonical class descriptor of the declaring module.

export class Endpoint {
  path: string;
}

export class ServerConfig {
  port: int;
  endpoint: Endpoint;
  tags?: string[];
  note?: string | null;
}

export function describe(s: ServerConfig): string;
