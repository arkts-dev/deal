// @spec: Arrays — read-only length
// @description: Deletion of xs.length is a compile-time error (E3017).
// @expected: compile-error E3017
// @features: arrays, diagnostics

export function test_length_delete(): null {
  let xs: int[] = [1, 2];
  delete xs.length;
  return null;
}
