# Frontend Conformance

Test lexing, parsing, checking, and module resolution independently of backend execution. Pin exact compile outcomes, diagnostics, and ranges against the specification. Keep support modules subordinate and cover accepted plus rejected forms. Keep this file always synchronized with code.
