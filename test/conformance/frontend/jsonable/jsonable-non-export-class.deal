// @spec: Classes — @jsonable directive placement
// @description: @jsonable attached to any declaration other than export class emits a warning and is ignored (v1.2 JSON serialization); the conformance runner cannot assert warnings, so this test verifies that no compilation error occurs.
// @expected: compile-ok
// @features: jsonable, warning

// @jsonable
class C {
  x: int = 0;
}

export function test_exists(): null {
  return null;
}

export function main(): null { return null; }
