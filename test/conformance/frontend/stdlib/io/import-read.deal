// @spec: Standard library declarations — std/io
// @description: std/io import is rejected (not a spec-listed stdlib module)
// @expected: compile-error E2003
// @features: stdlib, io

import * as io from "std/io"

export function test_io_read(path: string): string {
  return io.readText(path);
}
