// @spec: C FFI declaration files — C FFI externals entries
// @description: importing a C FFI declaration file (@extern-c) without a deal.json externals entry specifying nativeLibrary is rejected with E2010 at the import — the v1.2 C FFI manifest policy
// @expected: compile-error E2010
// @features: c-ffi, modules, v1.2

import * as ffi from "./ffi_math";

export function test_ffi_add(): int {
  return ffi.add(1, 2);
}

export function main(): null { return null; }
