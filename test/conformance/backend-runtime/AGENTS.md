# Runtime Conformance

Assert observable values, effects, errors, source origins, identity, and evaluation order across applicable backends. Execute production artifacts and keep helpers deterministic. Add cases that expose semantic-emitter divergence; never bless AST-only behavior. Keep this file always synchronized with code.
