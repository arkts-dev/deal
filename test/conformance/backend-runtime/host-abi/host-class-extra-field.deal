// @spec: Modules, declarations, standard library, and host ABI
// @description: A host-declared class literal that provides a declared optional field absent from the host <C>_defaults table raises E8007 at construction — the preserved defaults-map seam rejects the provided name at runtime (host-module-abi D2; the presence fixture's Config_defaults carries only port).
// @expected: runtime-error E8007
// @features: modules, host-abi, classes

import * as presence from "host/presence"

export function test_host_class_extra_field(): null {
  let c: presence.Config = {
    port: 9000,
    fallback: null,
  };
  return null;
}

export function main(): null {
  return null;
}
