// @spec: Arrays — Array writes
// @description: Negative array writes are out of bounds and raise E8002
// @expected: runtime-error E8002
// @features: arrays, indexing, runtime

export function test_array_negative_write(): int {
  let xs: int[] = [1, 2, 3];
  xs[-1] = 99;
  return xs[0];
}
