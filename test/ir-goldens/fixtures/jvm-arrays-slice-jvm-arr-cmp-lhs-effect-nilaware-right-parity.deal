// IR golden fixture: jvm-arrays-slice-jvm-arr-cmp-lhs-effect-nilaware-right-parity
// Migrated IR-containment pins from test/conformance/fixtures/jvm-arrays-slice.json case 'jvm-arr-cmp-lhs-effect-nilaware-right-parity' (ISSUE-0358).

import * as console from "std/console"
function markB(label: string, v: boolean): boolean { console.log(label); return v; }
export function test(): null {
  let bs: boolean[] = [true];
  if (markB("lhs", true) === (bs[-1] && true)) { console.log("bad"); }
}
