// IR golden fixture: jvm-arrays-slice-jvm-arr-cmp-lhs-effect-string-parity
// Migrated IR-containment pin for legacy case 'jvm-arr-cmp-lhs-effect-string-parity' (ISSUE-0358).

import * as console from "std/console"
function markS(label: string, v: string): string { console.log(label); return v; }
export function test(): null {
  let ss: string[] = ["a"];
  if (markS("lhsS", "x") === ss[-1]) { console.log("badS"); }
}
