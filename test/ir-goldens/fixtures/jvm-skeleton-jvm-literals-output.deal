// IR golden fixture: jvm-skeleton-jvm-literals-output
// Migrated IR-containment pins from test/conformance/fixtures/jvm-skeleton.json case 'jvm-literals-output' (ISSUE-0358).

import * as console from "std/console"
function intLit(): int { return 42; }
function numLit(): number { return 3.5; }
function strLit(): string { return "jvm-literals"; }
function boolLit(): boolean { return true; }
export function test(): string {
  let z: null = null;
  let i: int = intLit();
  let n: number = numLit();
  let b: boolean = boolLit();
  if (i === 42 && n === 3.5 && b) {
    console.log(strLit());
  } else {
    console.log("literals-broken");
  }
  return strLit();
}
