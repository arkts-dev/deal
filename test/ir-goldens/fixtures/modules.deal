// IR golden fixture: modules
// Tests module-level structure with imports and exports.

import * as Lib from "./lib/modules-helper"

function compute(x: int): int {
    return x + 1;
}

export function run(): int {
    let base: int = 10;
    return compute(base);
}
