// IR golden fixture: jvm-nullable-slice-jvm-nullable-int-locals-flow
// Migrated IR-containment pin for legacy case 'jvm-nullable-int-locals-flow' (ISSUE-0358).

export function test(): int {
  let n: int | null = 5;
  let m: int | null = null;
  let total: int = 0;
  if (n !== null) {
    total = n + 1;
  }
  if (m === null) {
    total = total + 1;
  } else {
    total = 99;
  }
  return total;
}
