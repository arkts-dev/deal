// IR golden fixture: jvm-semantic-slice-jvm-slice-boolean-comparisons
// Migrated IR-containment pins from test/conformance/fixtures/jvm-semantic-slice.json case 'jvm-slice-boolean-comparisons' (ISSUE-0358).

export function test(): boolean {
  let a: boolean = 3 < 5 && 5 <= 5 && 7 > 2 && 2 >= 2;
  let b: boolean = !(3 === 4) && 3 !== 4;
  let c: boolean = "alpha" < "beta" && "beta" !== "gamma";
  let d: boolean = false || true;
  return a && b && c && d;
}
