# Native Fixtures

Provide small deterministic Deal programs for native and runtime integration. Keep exports, signatures, and results stable; avoid unrelated features. Exercise real lowered host, call, and export boundaries. Keep this file always synchronized with code.
