# Shared Fixtures

Provide minimal reusable native, FFI, registry, and runtime inputs. Fixture names, symbols, and layout are test APIs; avoid unrelated behavior and unstated platform assumptions. Explain intent in consuming tests. Keep this file always synchronized with code.
