# Project Graph Fixtures

Model real manifests, roots, imports, declarations, cycles, and invalid graphs. Directory topology and raw paths are test input; never flatten or repair intentional defects. Preserve canonical identity relationships. Keep this file always synchronized with code.
