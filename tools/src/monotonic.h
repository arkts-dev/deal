/*
 * DEALPG4 monotonic deadline utility (stage placeholder).
 *
 * Timerfd absolute deadlines, EINTR recompute, phase recipes, and the
 * embedded-limits constants with ordering validation land in the
 * time-stream-utilities child.
 */
#ifndef DEALPG4_MONOTONIC_H
#define DEALPG4_MONOTONIC_H

/* Monotonic utility API lands in the time-stream-utilities child. */

#endif
