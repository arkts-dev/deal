# Source Coordinates

Own Unicode-scalar offsets, lines, columns, and serialized ranges. Centralize CR, LF, CRLF, supplementary-scalar, and malformed-surrogate handling; never duplicate UTF-16 arithmetic elsewhere. Keep recovery deterministic and test boundary cases. Keep this file always synchronized with code.
