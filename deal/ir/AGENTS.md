# Legacy IR Dump

Provide a deterministic observer over checked AST. This package is not semantic IR and owns no compilation behavior. Do not expand it into a backend contract; prefer semantic-IR validation and goldens for new lowering behavior. Keep this file always synchronized with code.
