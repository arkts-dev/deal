# Modules

Own module resolution, dependency order, checked-project orchestration, and identity assembly. Complete and validate the graph before lowering or publication. Preserve phase boundaries and deterministic order. Semantic routes consume validated project IR; never silently fall back within a compilation. Keep this file always synchronized with code.
