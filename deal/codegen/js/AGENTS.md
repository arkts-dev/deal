# JavaScript Backend

Preserve CommonJS ABI, source locations, runtime checks, and evaluation order. Current AST consumption is acceptable migration state, not architecture. Drive toward a semantic-IR emitter and retire AST walking after differential and end-to-end parity. Avoid deepening AST-only semantics. Architectural direction never expands assigned task scope. Keep this file always synchronized with code.
