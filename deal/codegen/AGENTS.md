# Code Generation

Translate validated semantic IR into target representation without redefining language semantics. Preserve boundaries, evaluation order, errors, and ABI behavior. The product goal is semantic-IR emission on every backend; extend shared contracts and retire AST-consuming emitters after parity. Architectural direction never expands assigned task scope. Keep this file always synchronized with code.
