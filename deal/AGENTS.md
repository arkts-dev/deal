# Compiler

Own compiler phases, runtimes, and CLI integration. Preserve specification semantics, deterministic diagnostics, and transactional publication. Checked facts lower into backend-neutral semantic IR; backend AST consumption is temporary migration debt. Put semantics before emission and verify affected phases. Keep this file always synchronized with code.
