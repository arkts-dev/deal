# Semantics

Own backend-neutral checked-project lowering and shared runtime meaning. Encode evaluation order, boundaries, failures, calls, classes, modules, and stdlib behavior once. Ultimate goal: product-enable semantic IR on every backend and retire AST-consuming emitters. Complete constructs vertically. Architectural direction never expands assigned task scope. Keep this file always synchronized with code.
