# Diagnostics

Own stable compiler diagnostics and structured rendering. Codes, severity, ranges, notes, ordering, and serialized bytes are observable contracts. Use canonical source-range facilities; never leak raw exceptions. Test malformed inputs and deterministic ordering. Keep this file always synchronized with code.
