# FFI

Own C declaration validation, ABI metadata, and binding plans. Validate closed type shapes before publication; preserve names and source order. Executable FFI semantics must move through explicit semantic-IR bindings. Unsupported targets reject deterministically. Keep this file always synchronized with code.
