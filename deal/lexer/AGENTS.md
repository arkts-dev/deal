# Lexer

Produce tokens, directives, and exact Unicode-scalar spans with deterministic recovery. Delegate position arithmetic to source utilities. Encode syntax only, never checker or backend policy. Update lexer, parser, and diagnostic tests together. Keep this file always synchronized with code.
