# Project Configuration

Own strict manifest parsing, project discovery, roots, outputs, and external declarations. Validate before overrides or output creation. Normalize paths once while preserving configured identity where required. Configuration failures remain deterministic and publish nothing. Keep this file always synchronized with code.
