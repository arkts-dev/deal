# Publication

Publish whole-project artifacts transactionally: stage, validate, then atomically replace. Never expose mixed generations or partial output. Preserve per-root locking and crash recovery; temporary nonces never enter artifact bytes. Exercise failure and restoration paths. Keep this file always synchronized with code.
