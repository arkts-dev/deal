# Identity

Own immutable canonical module and class identities. Preserve raw external specifiers where required. Keep resolution, path heuristics, descriptor rendering, and backend naming outside this package. Test structural equality and cross-module identity. Keep this file always synchronized with code.
