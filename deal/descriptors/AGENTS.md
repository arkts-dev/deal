# Descriptors

Own canonical runtime descriptors and matching. Maintain one strict Type-to-text authority, byte-stable rendering, and inverse parsing. Never invent local encodings; unsupported internal types fail before publication. Semantic IR carries canonical descriptors, never AST syntax. Keep this file always synchronized with code.
