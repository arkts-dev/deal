# Standard Library

Ship declarations and target implementations matching specification-visible signatures, Unicode behavior, effects, and failures. Keep Deal, Lua, JavaScript, JVM, and shared semantic algorithms equivalent where applicable. Preserve deterministic module surfaces and verify contracts across targets. Keep this file always synchronized with code.
